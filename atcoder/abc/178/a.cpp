#include <bits/stdc++.h>
using namespace std;
using lli = long long;

int main() {
    cin.tie(nullptr);
    ios::sync_with_stdio(false);

    // inputs
    int x;
    cin >> x;

    // output
    cout << (x ? "0\n" : "1\n");
    return 0;
}
