#include <algorithm>
#include <iostream>
#include <vector>

using std::make_pair;
using std::pair;

using std::max;
using std::min;
using std::sort;

using std::cin;
using std::cout;

using std::vector;

int main() {
    int a, b, c, d, e;
    cin >> a >> b >> c >> d >> e;
    if (a == 0) cout << "1\n";
    if (b == 0) cout << "2\n";
    if (c == 0) cout << "3\n";
    if (d == 0) cout << "4\n";
    if (e == 0) cout << "5\n";

    return 0;
}