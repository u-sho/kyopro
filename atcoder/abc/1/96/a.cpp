#include <iostream>

using namespace std;
int main() {
    cin.tie(nullptr);
    ios::sync_with_stdio(false);

    short a, b, c, d;
    cin >> a >> b >> c >> d;
    cout << b - c << "\n";

    return 0;
}
