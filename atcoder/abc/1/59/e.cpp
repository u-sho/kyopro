#include <bits/stdc++.h>
int main() {
    using namespace std;

    int h, w, k;
    cin >> h >> w >> k;
    char s[h][w];
    for (int i = 0; i < h; i++) {
        for (int j = 0; j < w; j++) {
            scanf("%c", &s[i][j]);
        }
    }

    return 0;
}